{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to BinBuffer docs","text":""},{"location":"#github-repo-github","title":"Github repo  GitHub.","text":""},{"location":"#quick-example","title":"Quick example","text":"<pre><code>local BinBuffer = require(path.to.BinBuffer)\n\nlocal my_buf = BinBuffer.create({size = 10}) -- &lt;- creating empty buffer, size grows automatically\nBinBuffer.alloc(my_buf, 20) -- &lt;- manual alloc, for more high perfomance\n\nBinBuffer.writers.addString(my_buf, \"This is a string\") -- &lt;- writers functions, this is what we need!\n\nlocal data = BinBuffer.read(my_buf) -- &lt;- reads buffer and returns table of buffer content\nprint(data) -- &lt;- {[1] = \"This is a string\"}\n\nlocal basic_buffer = BinBuffer.tobuffer(my_buf) -- &lt;- returns a BASIC luau buffer primitive\n\nlocal frombuffer_example = BinBuffer.frombuffer(basic_buffer) -- &lt;- returns a BinBuffer object from BASIC buffer primitive\n</code></pre>"},{"location":"api/reference/","title":"BinBuffer API Reference","text":""},{"location":"api/reference/#constants","title":"Constants","text":"<p>These constants represent type identifiers used internally and exposed publicly.</p> <ul> <li><code>NIL = 0,</code></li> <li><code>BOOLEAN = 1,</code></li> <li><code>NUMBER_I8 = 2,</code></li> <li><code>NUMBER_I16 = 3,</code></li> <li><code>NUMBER_I32 = 5,</code></li> <li><code>NUMBER_U8 = 6,</code></li> <li><code>NUMBER_U16 = 7,</code></li> <li><code>NUMBER_U32 = 9,</code></li> <li><code>NUMBER_F16 = 10,</code></li> <li><code>NUMBER_F24 = 11,</code></li> <li><code>NUMBER_F32 = 12,</code></li> <li><code>NUMBER_F64 = 13,</code></li> <li><code>STRING = 14,</code></li> <li><code>STRING_LONG = 15,</code></li> <li><code>VECTOR2 = 16,</code></li> <li><code>VECTOR3 = 17,</code></li> <li><code>COLOR3 = 18,</code></li> <li><code>UDIM = 19,</code></li> <li><code>UDIM2 = 20,</code></li> <li><code>CFRAME = 21,</code></li> <li><code>RECT = 22,</code></li> <li><code>NUMBER_RANGE = 23,</code></li> <li><code>NUMBER_SEQUENCE = 24,</code></li> <li><code>COLOR_SEQUENCE = 25,</code></li> <li><code>BRICK_COLOR = 26,</code></li> <li><code>TABLE = 27,</code></li> <li><code>VECTOR2F16 = 28,</code></li> <li><code>VECTOR3F16 = 29,</code></li> <li><code>VECTOR2F24 = 30,</code></li> <li><code>VECTOR3F24 = 31,</code></li> <li><code>VECTOR2I16 = 32,</code></li> <li><code>VECTOR3I16 = 3,</code></li> <li><code>CFRAMEF16U8 = 37,</code></li> <li><code>CFRAMEF24U8 = 38,</code></li> <li><code>CFRAMEF16U16 = 39,</code></li> <li><code>CFRAMEF24U16 = 40,</code></li> </ul>"},{"location":"api/reference/#utility-functions","title":"Utility Functions","text":"<p><code>bytes(n: number): number</code></p> <p>Returns the input unchanged; syntactic sugar for specifying buffer sizes in bytes.</p> <p><code>kilobytes(n: number): number</code></p> <p>Converts kilobytes to bytes (multiplies by 1024).</p> <p><code>megabytes(n: number): number</code></p> <p>Converts megabytes to bytes (multiplies by 1,048,576).</p>"},{"location":"api/reference/#buffer-management","title":"Buffer Management","text":"<pre><code>create(options: { size?: number, callback?: (buffer) -&gt; () }): Buffer\n</code></pre> <p>Creates a new BinBuffer instance. Default size is 32 bytes. Maximum buffer size is capped at 32 MB.</p> <pre><code>clear(buf: Buffer)\n</code></pre> <p>Resets the write offset and instance list, reallocating internal buffer to its original size.</p> <pre><code>destroy(buf: Buffer)\n</code></pre> <p>Destroys the buffer by clearing all internal references and setting it to nil.</p> <pre><code>flush(buf: Buffer): boolean\n</code></pre> <p>Copies the written data into a new buffer, invokes the callback with it, and clears the buffer. Returns <code>false</code> if nothing to flush or buffer is destroyed.</p> <pre><code>alloc(buf: Buffer, requiredBytes: number): boolean\n</code></pre> <p>Ensures the internal buffer can accommodate <code>requiredBytes</code>. Grows buffer using dynamic sizing strategy. Returns <code>false</code> if capacity exceeds maximum allowed size (64 megabytes).</p> <pre><code>tobuffer(buf: Buffer): buffer\n</code></pre> <p>Returns a copy of the internal buffer containing only the written data.</p> <pre><code>fromBuffer(buf: buffer): Buffer?\n</code></pre> <p>Reconstructs a BinBuffer from a raw buffer by analyzing its structure and extracting instance metadata. Returns <code>nil</code> if buffer is empty.</p>"},{"location":"api/reference/#serialization-writers","title":"Serialization (Writers)","text":"<p>All writer functions are accessible via the <code>writers</code> table. Each returns <code>true</code> on success or <code>false</code> if allocation fails.</p> <pre><code>writers.addNil(buf)\n</code></pre> <p>Writes a nil value.</p> <pre><code>writers.addBoolean(buf, value)\n</code></pre> <p>Writes a boolean.</p> <pre><code>writers.addNumber(buf, value)\n</code></pre> <p>Automatically selects the most compact numeric representation (integer or float, signed/unsigned, with optimized bit-widths).</p> <pre><code>writers.addU8(buf, value)\n</code></pre> <p>Writes an unsigned 8-bit integer.</p> <pre><code>writers.addU16(buf, value)\n</code></pre> <p>Writes an unsigned 16-bit integer.</p> <pre><code>writers.addU32(buf, value)\n</code></pre> <p>Writes an unsigned 32-bit integer.</p> <pre><code>writers.addI8(buf, value)\n</code></pre> <p>Writes a signed 8-bit integer.</p> <pre><code>writers.addI16(buf, value)\n</code></pre> <p>Writes a signed 16-bit integer.</p> <pre><code>writers.addI32(buf, value)\n</code></pre> <p>Writes a signed 32-bit integer.</p> <pre><code>writers.addF16(buf, value)\n</code></pre> <p>Writes a 16-bit floating-point number.</p> <pre><code>writers.addF24(buf, value)\n</code></pre> <p>Writes a 24-bit floating-point number.</p> <pre><code>writers.addF32(buf, value)\n</code></pre> <p>Writes a 32-bit floating-point number.</p> <pre><code>writers.addF64(buf, value)\n</code></pre> <p>Writes a 64-bit floating-point number.</p> <pre><code>writers.addString(buf, value)\n</code></pre> <p>Writes a string; uses <code>STRING</code> for lengths &lt; 256, otherwise <code>STRING_LONG</code>.</p> <pre><code>writers.addStringShort(buf, value)\n</code></pre> <p>Writes a short string (length &lt; 256) using 1-byte length header.</p> <pre><code>writers.addStringLong(buf, value)\n</code></pre> <p>Writes a long string (length \u2265 256) using 2-byte length header.</p> <pre><code>writers.addVector2(buf, value)\n</code></pre> <p>Writes a <code>Vector2</code> using three 32-bit floats.</p> <pre><code>writers.addVector3(buf, value)\n</code></pre> <p>Writes a <code>Vector3</code> using three 32-bit floats.</p> <pre><code>writers.addVector2F16(buf, value)\n</code></pre> <p>Writes a <code>Vector2</code> using two 16-bit floats (5 bytes total).</p> <pre><code>writers.addVector3F16(buf, value)\n</code></pre> <p>Writes a <code>Vector3</code> using three 16-bit floats (7 bytes total).</p> <pre><code>writers.addVector2F24(buf, value)\n</code></pre> <p>Writes a <code>Vector2</code> using two 24-bit floats (7 bytes total).</p> <pre><code>writers.addVector3F24(buf, value)\n</code></pre> <p>Writes a <code>Vector3</code> using three 24-bit floats (10 bytes total).</p> <pre><code>writers.addVector2I16(buf, value)\n</code></pre> <p>Writes a <code>Vector2int16</code> using two 16-bit signed integers.</p> <pre><code>writers.addVector3I16(buf, value)\n</code></pre> <p>Writes a <code>Vector3int16</code> using three 16-bit signed integers.</p> <pre><code>writers.addCFrame(buf, value)\n</code></pre> <p>Writes a <code>CFrame</code> as Euler angles (3\u00d7U16) + position (3\u00d7F32), total 19 bytes.</p> <pre><code>writers.addCFrameF16U8(buf, value)\n</code></pre> <p>Writes a <code>CFrame</code> with 8-bit Euler angles and 16-bit float positions (10 bytes).</p> <pre><code>writers.addCFrameF24U8(buf, value)\n</code></pre> <p>Writes a <code>CFrame</code> with 8-bit Euler angles and 24-bit float positions (13 bytes).</p> <pre><code>writers.addCFrameF16U16(buf, value)\n</code></pre> <p>Writes a <code>CFrame</code> with 16-bit Euler angles and 16-bit float positions (13 bytes).</p> <pre><code>writers.addCFrameF24U16(buf, value)\n</code></pre> <p>Writes a <code>CFrame</code> with 16-bit Euler angles and 24-bit float positions (16 bytes).</p> <pre><code>writers.addColor3(buf, value)\n</code></pre> <p>Writes a <code>Color3</code> as 3\u00d7U8 (RGB \u00d7255).</p> <pre><code>writers.addUDim(buf, value)\n</code></pre> <p>Writes a <code>UDim</code> as scale (I16 \u00d71000) and offset (I16).</p> <pre><code>writers.addUDim2(buf, value)\n</code></pre> <p>Writes a <code>UDim2</code> as two <code>UDim</code> values.</p> <pre><code>writers.addRect(buf, value)\n</code></pre> <p>Writes a <code>Rect</code> as four F32 values (Min/Max X/Y).</p> <pre><code>writers.addNumberRange(buf, value)\n</code></pre> <p>Writes a <code>NumberRange</code> as two F32 values (Min/Max).</p> <pre><code>writers.addNumberSequence(buf, value)\n</code></pre> <p>Writes a NumberSequence by encoding each keypoint as three U8 values (Time, Value, Envelope scaled to [0,255]).</p> <pre><code>writers.addColorSequence(buf, value)\n</code></pre> <p>Writes a <code>ColorSequence</code> by encoding each keypoint as U8 (Time) + 3\u00d7U8 (RGB).</p> <pre><code>writers.addBrickColor(buf, value)\n</code></pre> <p>Writes a <code>BrickColor</code> by storing its numeric ID as U16.</p> <pre><code>writers.addTable(buf, value)\n</code></pre> <p>Recursively serializes a table as alternating key-value pairs, terminated by a <code>NIL</code> marker.</p>"},{"location":"api/reference/#deserialization","title":"Deserialization","text":"<pre><code>read(buf: Buffer): {any}\n</code></pre> <p>Deserializes the entire buffer into a list of reconstructed values. Resolves stored Instance references and supports all serialized types including nested tables.</p>"},{"location":"guides/installation/","title":"Installation","text":""},{"location":"guides/installation/#method-1-roblox-marketplace-recommended","title":"Method 1: Roblox Marketplace (Recommended)","text":""},{"location":"guides/installation/#download-from-the-roblox-marketplace","title":"Download from the Roblox Marketplace","text":"<p>Go to Roblox Marketplace</p> <p>Click \"Get Module\" to obtain the ModuleScript</p> <p>Insert it into ReplicatedStorage or another convenient folder</p>"},{"location":"guides/installation/#method-2-github-repository","title":"Method 2: GitHub Repository","text":"<p>Clone or download from GitHub Repository</p> <pre><code>-- Clone or download the repository:\n-- https://github.com/GameF200/BinBuffer\n-- File: BinBuffer.lua\n</code></pre>"},{"location":"guides/installation/#method-3-wally-coming-soon","title":"Method 3: Wally (Coming Soon)","text":"<p>Add to your wally.toml (available soon):</p> <pre><code>[dependencies]\nBinBuffer = \"gamef200/binbuffer@1.0.0\"\n</code></pre>"}]}